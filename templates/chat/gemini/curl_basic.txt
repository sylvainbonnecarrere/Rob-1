curl "https://generativelanguage.googleapis.com/v1beta/models/{{LLM_MODEL}}:generateContent" \
  -H "x-goog-api-key: {{API_KEY}}" \
  -H 'Content-Type: application/json' \
  -d '{
    "system_instruction": {
      "parts": [
        {
          "text": "{{SYSTEM_PROMPT_ROLE}} {{SYSTEM_PROMPT_BEHAVIOR}}"
        }
      ]
    },
    "contents": [
      {
        "parts": [
          {
            "text": "{{USER_PROMPT}}"
          }
        ]
      }
    ]
  }'
