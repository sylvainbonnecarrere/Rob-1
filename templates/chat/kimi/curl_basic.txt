curl https://openrouter.ai/api/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {{API_KEY}}" \
  -d '{
  "model": "{{LLM_MODEL}}",
  "messages": [
    {
      "role": "user",
      "content": "{{USER_PROMPT}}"
    },
    { 
      "role": "assistant", 
      "content": "{{SYSTEM_PROMPT_ROLE}}" 
    }
  ]
  
}'